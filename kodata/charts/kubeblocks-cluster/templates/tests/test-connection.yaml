apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubeblocks-cluster.fullname" . }}-test-connection"
  labels:
    {{- include "kubeblocks-cluster.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubeblocks-cluster.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
