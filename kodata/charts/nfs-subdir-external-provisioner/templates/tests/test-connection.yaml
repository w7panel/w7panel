apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nfs-subdir-external-provisioner.fullname" . }}-test-connection"
  labels:
    {{- include "nfs-subdir-external-provisioner.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nfs-subdir-external-provisioner.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
