# W7Panel UI 菜单地图

**文档版本**: 1.0  
**更新日期**: 2026-02-20  
**用途**: UI自动化测试 - 菜单定位参考

---

## 一、菜单结构概览

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  顶部导航栏 (Top Navigation)                                                 │
│  ┌─────────────┬─────────────────────────────────────────┬───────────────┐  │
│  │ 云主机      │ [应用组1] [应用组2] ...                  │ 系统管理 | 多租户管理 │  │
│  └─────────────┴─────────────────────────────────────────┴───────────────┘  │
├─────────────────────────────────────────────────────────────────────────────┤
│  左侧边栏 (Left Sidebar) - 主菜单                                            │
│  ┌─────────────────────────────────────────────────────────┐                │
│  │ 集群管理  ● 云主机                                      │                │
│  │   ├─ 概览                                              │                │
│  │   ├─ 节点管理                                          │                │
│  │   ├─ 资源对象浏览器                                    │                │
│  │   └─ 文件管理                                          │                │
│  │ 应用管理                                               │                │
│  │   ├─ 应用列表                                          │                │
│  │   ├─ 计划任务                                          │                │
│  │   ├─ 反向代理                                          │                │
│  │   └─ 集群数据库                                        │                │
│  │ 存储管理                                               │                │
│  │   ├─ 存储设备                                          │                │
│  │   └─ 存储分区                                          │                │
│  │ 制品管理                                               │                │
│  └─────────────────────────────────────────────────────────┘                │
├─────────────────────────────────────────────────────────────────────────────┤
│  右侧用户菜单 (Top Right Menu)                                              │
│  ┌─────────────────────────────────────────────────────────┐                │
│  │ [头像] admin                                           │                │
│  │  ├─ 修改密码                                           │                │
│  │  └─ 退出登录                                           │                │
│  └─────────────────────────────────────────────────────────┘                │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 路由说明

**重要**：系统使用 Vue Router 的 `createWebHistory` 模式（HTML5 History API），URL 格式为：
- 正确格式：`/app/apps`（无 #）
- 错误格式：`#/app/apps`（hash 模式，本系统不使用）

**微前端环境注意**：部分微前端环境可能会添加 `#` 前缀，但实际路由应为标准路径格式。

---

## 二、菜单地图详解

### 2.1 顶部导航栏 (Top Navigation Bar)

**位置**: 页面顶部，logo 右侧

| 菜单项 | 路由 | 说明 |
|--------|------|------|
| 云主机 | `/cluster/panel` | 默认首页，切换到云主机视图 |
| [应用组] | `/appgroup/{group}` | 动态应用组 (根据配置显示) |
| 系统管理 | `/system/cloud` | 点击后切换到系统管理视图 |
| 多租户管理 | `/usermanage/users` | 点击后切换到多租户管理视图 |

**定位方式**:
```bash
# 点击云主机
agent-browser click "text=云主机"

# 点击应用组
agent-browser click "text={应用组名称}"
```

---

### 2.2 左侧边栏 - 集群管理

**一级菜单**: 集群管理  
**图标**: `icon-common`  
**路由**: `/cluster`

| 二级菜单 | 路由 | 可见性 | 说明 |
|----------|------|--------|------|
| 概览 | `/cluster/panel` | ✅ 显示 | 集群仪表盘 |
| 节点管理 | `/cluster/nodes` | ✅ 显示 | K8s 节点列表 |
| 资源对象浏览器 | `/cluster/resource` | ✅ 显示 | K8s 资源浏览 |
| 文件管理 | `/cluster/nodes-files` | ❌ 隐藏 | 节点文件管理 |

**定位方式**:
```bash
# 点击节点管理
agent-browser click "text=节点管理"

# 点击资源对象浏览器
agent-browser click "text=资源对象浏览器"
```

---

### 2.3 左侧边栏 - 应用管理

**一级菜单**: 应用管理  
**图标**: `icon-apps`  
**路由**: `/app`

| 二级菜单 | 路由 | 可见性 | 说明 |
|----------|------|--------|------|
| 应用列表 | `/app/apps` | ✅ 显示 | 应用列表页面 |
| 计划任务 | `/app/cronjob` | ✅ 显示 | CronJob 管理 |
| 反向代理 | `/app/rvproxy` | ✅ 显示 | 反向代理配置 |
| 集群数据库 | `/app/dblist` | ✅ 显示 | 数据库集群 |

**定位方式**:
```bash
# 点击应用列表
agent-browser click "text=应用列表"

# 点击计划任务
agent-browser click "text=计划任务"
```

---

### 2.4 应用详情页 - 二级侧边栏

**路径**: `/app/appgroup/{group}/{kind}/{id}`  
**说明**: 点击应用列表中的某个应用后，展开的二级侧边栏

| 二级菜单 | 路由参数 | 说明 |
|----------|----------|------|
| 应用详情 | `detail` | 应用基本信息 |
| 监控 | `moniter` | 应用监控数据 |
| 容器列表 | `pod` | **容器/Pod 列表** |
| WebShell | `pod-webshell` | 容器终端 |
| 域名 | `domain` | 域名配置 |
| 事件 | `events` | 事件记录 |
| 历史版本 | `version` | 版本历史 |
| 文件管理 | `files` | 应用文件管理 |
| 执行脚本 | `job` | 作业/脚本执行 |

**定位方式**:
```bash
# 步骤1: 进入应用列表
agent-browser open "http://localhost:8080/app/apps"
sleep 3

# 步骤2: 点击某个应用 (例如 "testphp")
agent-browser click "text=testphp"
sleep 3

# 步骤3: 在展开的侧边栏中点击"容器列表"
agent-browser click "text=容器列表"
```

---

### 2.4.1 容器列表页面 - 详细操作

**路径**: `/app/appgroup/{group}/deployments/{id}/pod`

#### 页面结构

```
┌─────────────────────────────────────────────────────────────────────┐
│ 工具栏: [复制] [销毁重建] [自动刷新: ○]                             │
├─────────────────────────────────────────────────────────────────────┤
│ 表格:                                                                │
│ ┌──┬──┬────────────────────┬────────┬──────────┬──────────┬──────┐ │
│ │ + │ ☐ │ 实例名称          │ 状态   │ 容器IP   │ 节点IP   │ 操作 │ │
│ ├──┼──┼────────────────────┼────────┼──────────┼──────────┼──────┤ │
│ │ + │ ☐ │ testphp-xxx      │Running │10.42.0.7 │10.0.0.206│ 📄 🗑│ │
│ └──┴──┴────────────────────┴────────┴──────────┴──────────┴──────┘ │
└─────────────────────────────────────────────────────────────────────┘
```

#### 交互元素列表

| 元素 | 定位方式 | 说明 |
|------|----------|------|
| 复制按钮 | `button "复制"` 或 `@e1` | 复制选中应用的标签 |
| 销毁重建按钮 | `button "销毁重建"` | 需先勾选容器才能启用 |
| 自动刷新开关 | `switch` | 切换自动刷新 |
| 展开/收起 | `.arco-table-expand-btn` | 展开/收起行详情 |
| 全选checkbox | `checkbox "全选"` | 选择所有容器 |
| Pod名称 | `.c-blue.cursor` | 显示Pod名称 |
| 复制图标 | `.arco-icon-copy` | 复制Pod名称到剪贴板 |
| 运行状态 | `.c-green.cursor` | Running状态(绿色) |
| 日志图标 | `.arco-icon-file` | 查看容器日志 |
| 删除图标 | `.arco-icon-delete` | 销毁重建容器 |

#### 详细测试步骤

```bash
# 1. 进入容器列表页面
agent-browser open "http://localhost:8080/app/appgroup/xugnofjf/deployments/testphp-xugnofjf/pod"
sleep 5

# 2. 获取页面元素
agent-browser snapshot -i

# 3. 测试复制按钮
agent-browser click "text=复制"
sleep 1
# 检查是否显示消息 "请选择要复制应用的标签"

# 4. 测试展开/收起功能
agent-browser eval "document.querySelector('.arco-table-expand-btn')?.click()"
sleep 1
# 检查是否显示容器详情 (容器名称、ID、镜像、CPU、内存等)

# 5. 测试勾选容器 (重要！勾选后才能启用销毁重建)
agent-browser eval "document.querySelector('.arco-checkbox-target')?.click()"
sleep 1

# 6. 检查销毁重建按钮是否启用
agent-browser snapshot -i
# 应该看到 button "销毁重建" 不再有 [disabled]

# 7. 测试日志查看器
agent-browser eval "document.querySelector('.arco-icon-file')?.click()"
sleep 1
# 检查是否显示日志弹窗

# 8. 测试销毁重建
agent-browser click "text=销毁重建"
sleep 1
# 检查是否显示确认对话框 "是否确定销毁容器并重建"
```

#### 关键测试点

| 测试项 | 前置条件 | 预期结果 |
|--------|----------|----------|
| 复制按钮 | 无 | 点击后显示"请选择要复制应用的标签" |
| 销毁重建 | **需先勾选容器** | 勾选前禁用，勾选后启用 |
| 自动刷新 | 无 | 切换开关状态 |
| 展开详情 | 无 | 显示容器详细信息 |
| 日志查看 | 无 | 打开日志弹窗 |
| 删除确认 | 需勾选容器 | 显示确认对话框 |

#### 常见问题

1. **销毁重建按钮始终禁用**
   - 原因：需要先勾选容器列表
   - 解决：先用 checkbox 勾选容器

2. **点击无反应**
   - 可能页面未完全加载
   - 解决：增加 sleep 时间，或使用 eval 执行点击

3. **找不到元素**
   - 元素可能是 SVG 图标
   - 解决：使用 `snapshot -i -C` 获取所有可点击元素

---

### 2.5 左侧边栏 - 存储管理

**一级菜单**: 存储管理  
**图标**: `icon-storage`  
**路由**: `/storage`

| 二级菜单 | 路由 | 可见性 | 说明 |
|----------|------|--------|------|
| 存储设备 | `/storage/disk` | ✅ 显示 | Longhorn 存储设备 |
| 存储分区 | `/storage/zone` | ✅ 显示 | 存储卷 |

**定位方式**:
```bash
# 点击存储设备
agent-browser click "text=存储设备"
```

---

### 2.6 左侧边栏 - 制品管理

**一级菜单**: 制品管理  
**图标**: `icon-package`  
**路由**: `/zpk`

| 二级菜单 | 路由 | 可见性 | 说明 |
|----------|------|--------|------|
| 制品库 | `/zpk/list` | ✅ 显示 | 制品仓库 |

**定位方式**:
```bash
# 点击制品库
agent-browser click "text=制品库"
```

---

### 2.7 右侧用户菜单 (User Menu)

**位置**: 页面右上角，头像旁边

| 菜单项 | 说明 |
|--------|------|
| 修改密码 | 打开修改密码对话框 |
| 退出登录 | 退出当前账号 |

**定位方式**:
```bash
# 点击用户头像/用户名
agent-browser click ".arco-avatar"
# 或
agent-browser click "text=admin"

# 点击修改密码
agent-browser click "text=修改密码"

# 点击退出登录
agent-browser click "text=退出登录"
```

---

### 2.8 顶部功能按钮

**位置**: 顶部导航栏右侧

| 按钮 | 功能 | 定位方式 |
|------|------|----------|
| 主题切换 | 切换深色/浅色主题 | `agent-browser click ".icon-moon-fill"` 或 `.icon-sun-fill` |
| 全屏 | 切换全屏模式 | `agent-browser click ".icon-fullscreen"` |

---

## 三、菜单层级关系图

```
根菜单
├── 集群管理 (/cluster)
│   ├── 概览 (/cluster/panel)
│   ├── 节点管理 (/cluster/nodes)
│   ├── 资源对象浏览器 (/cluster/resource)
│   └── 文件管理 (/cluster/nodes-files) [隐藏]
│
├── 应用管理 (/app)
│   ├── 应用列表 (/app/apps)
│   │   └── [应用详情页] (/app/appgroup/{group}/{kind}/{id})
│   │       ├── 应用详情 (detail)
│   │       ├── 监控 (moniter)
│   │       ├── 容器列表 (pod) ◄── 容器列表入口
│   │       ├── WebShell (pod-webshell)
│   │       ├── 域名 (domain)
│   │       ├── 事件 (events)
│   │       ├── 历史版本 (version)
│   │       ├── 文件管理 (files)
│   │       └── 执行脚本 (job)
│   │
│   ├── 计划任务 (/app/cronjob)
│   ├── 反向代理 (/app/rvproxy)
│   └── 集群数据库 (/app/dblist)
│
├── 存储管理 (/storage)
│   ├── 存储设备 (/storage/disk)
│   └── 存储分区 (/storage/zone)
│
└── 制品管理 (/zpk)
    └── 制品库 (/zpk/list)
```

---

## 四、常见测试场景

### 4.1 测试容器列表

```bash
# 完整路径: 应用管理 → 应用列表 → 某个应用 → 容器列表
agent-browser open "http://localhost:8080/app/apps"
sleep 3

# 1. 点击应用列表中的某个应用
agent-browser click "text=testphp"
sleep 3

# 2. 在展开的二级侧边栏中点击"容器列表"
agent-browser click "text=容器列表"
sleep 3

# 3. 验证页面显示容器信息
agent-browser eval "document.body.innerText"
```

### 4.2 测试节点管理

```bash
# 路径: 集群管理 → 节点管理
agent-browser open "http://localhost:8080/cluster/nodes"
sleep 3

# 验证节点信息
agent-browser eval "document.body.innerText"
```

### 4.3 测试存储设备

```bash
# 路径: 存储管理 → 存储设备
agent-browser open "http://localhost:8080/storage/disk"
sleep 3

# 验证存储信息
agent-browser eval "document.body.innerText"
```

### 4.4 测试资源对象浏览器

```bash
# 路径: 集群管理 → 资源对象浏览器
agent-browser open "http://localhost:8080/cluster/resource"
sleep 3

# 选择资源类型 (例如 core -> Pod)
agent-browser click "text=core"
sleep 1
agent-browser click "text=Pods"
sleep 2
```

---

## 五、UI测试快速定位方法

### 5.1 获取当前页面所有菜单项

```bash
# 方法1: 获取所有链接文本
agent-browser eval "Array.from(document.querySelectorAll('.arco-menu-item, .arco-menu-submenu-title')).map(el => el.textContent.trim())"

# 方法2: 获取所有可点击元素
agent-browser snapshot -i
```

### 5.2 通过URL直接访问

| 功能 | 直接URL |
|------|---------|
| 容器列表 | `/app/appgroup/{group}/pod/{appId}` |
| 节点管理 | `/cluster/nodes` |
| 资源浏览器 | `/cluster/resource` |
| 存储设备 | `/storage/disk` |

---

## 六、注意事项

1. **菜单动态性**: 部分菜单项 (如应用组) 是动态从后端获取的
2. **权限控制**: 根据用户权限，某些菜单可能不显示
3. **页面加载**: 切换菜单后需要适当等待 (`sleep 2-3`)
4. **应用详情**: 容器列表位于应用详情页的二级侧边栏，需先点击应用

---

## 七、附录: 路由文件位置

| 路由模块 | 文件路径 |
|----------|----------|
| 集群管理 | `w7panel-ui/src/router/routes/modules/cluster.ts` |
| 应用管理 | `w7panel-ui/src/router/routes/modules/app.ts` |
| 存储管理 | `w7panel-ui/src/router/routes/modules/storage.ts` |
| 制品管理 | `w7panel-ui/src/router/routes/modules/zpk.ts` |
| 系统管理 | `w7panel-ui/src/router/routes/modules/system.ts` |

---

*文档更新: 每次菜单结构变更后需同步更新此文档*
